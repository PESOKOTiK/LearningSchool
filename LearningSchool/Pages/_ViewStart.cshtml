@{
    var role = Context.Session.GetString("UserRole");
    if (string.IsNullOrEmpty(role) && !Context.Request.Path.Value.Contains("/Login") && !Context.Request.Path.Value.Contains("/Register"))
    {
        Context.Response.Redirect("/Login");
    }
    Layout = "_Layout";
}